apiVersion: v1
kind: Service
metadata:
  namespace: qa
  name: green-service
  labels:
    app: green-app
spec:
  type: NodePort
  selector:
    app: green-app
  ports:
    - port: 80
      targetPort: 80